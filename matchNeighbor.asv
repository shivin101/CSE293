function []=matchNeighbor(B,I,matchNN,matchDist,)
matchId = I(1);
    if vis(I(1))==1
        for i=1:size(matchNN,1)
            if(matchNN(i))==I(1)
                if(matchDist())
            end
        end
    else
        vis(I((1))=1;
        matchNN(idx)=I(1);
        matchDist(idx)=B(1);
    end
end